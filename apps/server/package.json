{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "main": "dist/index.js",
  "dependencies": {
    "@repo/game-engine": "*",
    "@repo/shared": "*",
    "express": "^4.18.0",
    "socket.io": "^4.7.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
